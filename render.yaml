services:
  - type: web
    name: renewable-flexibility-studio
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server
    healthCheckPath: /
    autoDeployTrigger: checksPass
    renderSubdomainPolicy: enabled
